node_modules/
*.log
.DS_Store

# Simulation output stays local. Working out which cards are strong is most of
# the fun of playing the game, so results are deliberately not published here.
results*.json
weights*.json
fitted*.json
corpus*.jsonl
